# Frontend Configuration
VITE_API_URL=http://localhost:3000

# For local testing with MinIO (included in docker-compose):
VITE_METADATA_URL=http://localhost:9000/xlsforms/metadata.json

# For production with AWS S3, use:
# VITE_METADATA_URL=https://xlsforms.s3.amazonaws.com/metadata.json

# Backend Configuration
PORT=3000

# Storage Configuration
# For local testing with MinIO (default in docker-compose):
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET_NAME=xlsforms
S3_ENDPOINT=http://minio:9000
S3_EXTERNAL_ENDPOINT=http://localhost:9000
S3_USE_PATH_STYLE=true

# For production with AWS S3, use:
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# S3_BUCKET_NAME=xlsforms
# (Remove S3_ENDPOINT, S3_EXTERNAL_ENDPOINT, and S3_USE_PATH_STYLE)
